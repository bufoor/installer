---
- name: Install 
  apt: name="{{ item }}" state=present
  with_items:
   - python-selinux
   - mc
   - network-manager-openconnect
   - software-properties-common
   - gnome-shell
   - gnome-tweak-tool
   - vlc
   - firewalld
   - firewall-config

